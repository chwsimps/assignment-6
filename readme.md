# Used Javascript to Access Information within Arrays

Used the items variable, such as array.map(), array.reduce(), array.forEach, and array.filter() to help solve problem involving information within arrays.

How to view:

* Clone my repo
* Run npm install
* Run bower install
